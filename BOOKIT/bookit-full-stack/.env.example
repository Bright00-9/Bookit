# Shared across services via docker-compose.yml. Copy to .env and fill in
# real values before running `docker compose up` - never commit the real
# .env, only this .example file.
POSTGRES_USER=bookit
POSTGRES_PASSWORD=bookit
POSTGRES_DB=bookit

JWT_SECRET=change-me-to-a-long-random-string

VITE_API_BASE_URL=http://localhost:8080
